
spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 
    url: jdbc:mysql://localhost:3306/my_pay_v1?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong

  thymeleaf:
    cache: false

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
  servlet:
    context-path: /union-pay